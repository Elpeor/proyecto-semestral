apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-despacho
  namespace: tienda
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend-despacho
  template:
    metadata:
      labels:
        app: backend-despacho
    spec:
      containers:
        - name: backend-despacho
          image: 022983448261.dkr.ecr.us-east-1.amazonaws.com/backend-despacho:latest
          ports:
            - containerPort: 3002
          env:
          - name: DB_URL
            value: jdbc:mysql://db:3306/tienda
          - name: DB_USER
            value: root
          - name: DB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: mysql-secret
                key: MYSQL_ROOT_PASSWORD

          # Se agrega Resource para que Metrics Server calcule el % de CPU y Memoria para escalar 
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          # HEALTH CHECKS (PROBES)
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 3002
            failureThreshold: 30
            periodSeconds: 5

          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 3002
            initialDelaySeconds: 40
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 3002
            initialDelaySeconds: 60
            periodSeconds: 20